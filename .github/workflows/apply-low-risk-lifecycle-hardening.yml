name: Apply low-risk lifecycle hardening

on:
  push:
    branches:
      - experimental
    paths:
      - .github/workflows/apply-low-risk-lifecycle-hardening.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: experimental
          fetch-depth: 0

      - name: Apply deterministic source changes
        run: python3 .github/scripts/apply_low_risk_lifecycle_hardening.py

      - name: Validate and commit
        run: |
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/scripts/apply_low_risk_lifecycle_hardening.py
          git rm .github/workflows/apply-low-risk-lifecycle-hardening.yml
          git add QtScrcpy
          git commit -m "Harden server cleanup and tunnel port allocation"
          git push origin HEAD:experimental
